const requestLogger = require('./request.logger');
const responseLogger = require('./response.logger');
const schemaLogger = require('./schema.logger');

module.exports = {
  requestLogger,
  responseLogger,
  schemaLogger,
};
